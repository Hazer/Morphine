//apply plugin: 'kotlin-kapt'

sourceSets {
    main.java.srcDirs += "${buildDir.absolutePath}/generated/source/kaptKotlin/main/"
}

dependencies {
    implementation project(Modules.libs.sulfate.annotations)
    kapt project(Modules.libs.sulfate.erased)
}