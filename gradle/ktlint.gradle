apply plugin: "org.jlleitschuh.gradle.ktlint"

ktlint {
    version = "0.31.0"
    debug = true
    android = true
    outputToConsole = true
    filter {
        exclude("**/generated/**")
        include("**/kotlin/**")
    }
}